<h1>My Assignments</h1>

<%= "Loaded at #{@time}" %>
<h2>Private Repos</h2>
<% @client.org_repos('project-camelback', :type => 'private').each do |repo| %>
      <%= "<a href='https://github.com/#{repo.full_name}'>#{repo.name}</a><br>" %>
<% end %>
<!-- <h2>Public Repos</h2>
 --><%# @client.org_repos(GITHUB_ORG, :type => 'public').each do |repo| %>
      <%# "<a href='https://github.com/#{repo.full_name}'>#{repo.name}</a><br>" %>
<%# end %>